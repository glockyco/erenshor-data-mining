# Cloudflare Web Analytics beacon token (public, safe to expose to client).
# Empty by default so clean checkouts type-check and build without analytics.
# Set the real token in .env.local, which stays untracked.
PUBLIC_CLOUDFLARE_ANALYTICS_TOKEN=
